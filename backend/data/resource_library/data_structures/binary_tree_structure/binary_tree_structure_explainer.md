# 二叉树结构基础

## 适用对象
正在学习数据结构，初次接触二叉树概念的学生。

## 学习目标
- 理解二叉树的定义和基本术语
- 掌握二叉树的分类（满二叉树、完全二叉树）
- 了解二叉树的两种存储方式
- 能够使用代码定义二叉树节点

## 核心概念

### 什么是二叉树
二叉树是一种树形数据结构，每个节点最多有两个子节点，分别称为左子节点和右子节点。

### 基本术语

| 术语 | 定义 |
|------|------|
| **根节点** | 树的最顶层节点，没有父节点 |
| **叶子节点** | 没有子节点的节点 |
| **父节点** | 有子节点的节点 |
| **子节点** | 被父节点直接包含的节点 |
| **兄弟节点** | 拥有相同父节点的节点 |
| **节点的度** | 节点拥有的子节点数（0、1 或 2） |
| **树的深度** | 从根节点到最远叶子节点的路径长度 |
| **节点的高度** | 从该节点到最远叶子节点的路径长度 |

### 二叉树的分类

#### 1. 满二叉树
- 所有非叶子节点都有两个子节点
- 所有叶子节点都在同一层

#### 2. 完全二叉树
- 除最后一层外，其他层的节点数都达到最大
- 最后一层的节点都集中在左边

#### 3. 二叉搜索树（BST）
- 左子树的所有节点值小于根节点值
- 右子树的所有节点值大于根节点值

### 二叉树的性质

1. **第 i 层的最大节点数**：2^(i-1)（i ≥ 1）
2. **深度为 k 的二叉树最大节点数**：2^k - 1
3. **n0 = n2 + 1**：叶子节点数 = 度为2的节点数 + 1

## 存储方式

### 1. 链式存储（最常用）

```python
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val      # 节点值
        self.left = left    # 左子节点
        self.right = right  # 右子节点

# 创建二叉树
root = TreeNode(1)
root.left = TreeNode(2)
root.right = TreeNode(3)
root.left.left = TreeNode(4)
root.left.right = TreeNode(5)
```

### 2. 数组存储（适用于完全二叉树）

对于完全二叉树，可以用数组按层序存储：
- 索引为 i 的节点：
  - 左子节点：2*i + 1
  - 右子节点：2*i + 2
  - 父节点：(i-1) // 2

```python
# 层序遍历的数组表示
#        1
#       / \
#      2   3
#     / \
#    4   5
tree_array = [1, 2, 3, 4, 5]
```

## 常见错误

### 错误 1：混淆深度和高度
```python
# 深度：从根到当前节点的路径长度
# 高度：从当前节点到叶子的路径长度
```

### 错误 2：忘记空树处理
```python
# 空树的根节点为 None
root = None  # 这是合法的空二叉树
```

### 错误 3：数组存储时索引计算错误
```python
# 正确的索引关系
parent = (i - 1) // 2
left_child = 2 * i + 1
right_child = 2 * i + 2
```

## 自我检查
1. 什么是满二叉树？什么是完全二叉树？
2. 深度为 3 的满二叉树有多少个节点？
3. 如果一个二叉树有 10 个叶子节点，度为 2 的节点有多少个？
4. 用数组存储完全二叉树时，索引为 3 的节点的父节点索引是多少？

## 下一步建议
学习二叉树的遍历方式（前序、中序、后序、层序）和常见操作（插入、删除、查找）。